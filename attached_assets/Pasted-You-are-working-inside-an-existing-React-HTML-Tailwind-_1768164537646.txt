You are working inside an existing React + HTML + Tailwind single-file project.
DO NOT rewrite the UI.
DO NOT remove or rename any existing components, states, or form fields.
DO NOT touch any external API keys or ML/AQI logic already present.

GOAL:
Make the blockchain implementation FULLY FUNCTIONAL and CONSISTENT for BOTH:
1) Citizen side (complaint submission)
2) Authority side (status updates)

CURRENT ISSUES TO FIX:
- Blockchain data is not reliably synced between citizen and authority views
- Complaints and status updates must be immutable
- Blockchain persistence must survive refresh
- Authority actions must append new blocks instead of modifying old data

REQUIRED IMPLEMENTATION RULES:

1) Use the existing Block and Blockchain classes.
   - Do NOT remove them
   - Improve them only if required

2) Blockchain behavior:
   - Every complaint submission → NEW BLOCK
   - Every authority status update → NEW BLOCK (type = "status_update")
   - NEVER mutate existing complaint blocks
   - Complaint status must be derived by replaying the chain

3) Persistence:
   - Store blockchain.chain in localStorage after EVERY block addition
   - Load blockchain.chain from localStorage on app init
   - Recalculate complaint list by iterating over the chain

4) Citizen side:
   - On submit:
     - Add a complaint block
     - Reload blockchain from memory
     - Recompute complaints from chain
   - Show only derived complaint state (not raw blocks)

5) Authority side:
   - Status change must:
     - Add a NEW block with complaintId reference
     - Never modify original complaint data
   - UI must instantly reflect updated status after mining

6) Data integrity:
   - Add chain validation check before rendering complaints
   - If chain is invalid → show warning banner (do not crash)

7) Performance:
   - Keep mining difficulty LOW (<=2)
   - Avoid infinite loops or UI blocking

8) Output:
   - Final code must RUN without errors
   - No console errors
   - No UI regressions
   - Blockchain must work identically for both portals

IMPORTANT:
- DO NOT convert this into a real crypto network
- This is a client-side immutable ledger simulation
- Keep everything browser-compatible

Implement ONLY what is necessary to make the blockchain functional, persistent, and correct.
