I already have an ML-based pollution attribution engine that dynamically computes,
per ward:
- Primary pollutant
- Secondary pollutants
- Normalized contribution scores
- Overall pollution severity
- Allowed mitigation controls

Your task is to make the AI Intelligence layer FULLY DYNAMIC
and driven strictly by ML outputs.

────────────────────────────────────
CRITICAL RULES (DO NOT VIOLATE)
────────────────────────────────────
1. Do NOT recalculate pollution logic in the UI or LLM
2. Do NOT hardcode primary pollutants or actions
3. Do NOT make AQI or ward data static
4. Do NOT break real-time or periodic AQI updates
5. Do NOT override ML engine outputs with AI reasoning

The ML engine is the single source of truth.

────────────────────────────────────
FUNCTIONAL REQUIREMENTS
────────────────────────────────────

1️⃣ Ward-wise Primary Pollutant (Dynamic)
- Read primary_pollutant directly from ML engine output
- Display it per ward in the Intelligence panel
- Support multiple dominant pollutants if provided

2️⃣ AI Intelligence Panel
- Must render without runtime errors
- Must update automatically when ML output changes
- Must not contain pollution calculation logic
- Only interpretation, explanation, and planning logic

3️⃣ 90-Day Execution Plan (Dynamic)
Generate a ward-specific 90-day plan based on:
- Primary pollutant(s)
- Severity level
- Allowed control actions provided by ML engine

The 90-day plan must be structured as:
- Days 0–30: Immediate actions
- Days 31–60: Enforcement & scaling
- Days 61–90: Monitoring & optimization

Actions must be selected ONLY from the provided control library.
No new actions should be invented.

4️⃣ AQI Dynamism Protection
- updatePollutionData / scheduled updates must remain intact
- AI layer must react to new data, not block it
- UI must re-render when ward data updates

────────────────────────────────────
ARCHITECTURE EXPECTATION
────────────────────────────────────
APIs → ML Attribution Engine → Intelligence Layer → UI

The AI Intelligence layer must:
- Consume ML output as input
- Never act as a predictor
- Never modify AQI computation

────────────────────────────────────
OUTPUT FORMAT (STRICT)
────────────────────────────────────
All AI outputs must be structured JSON, suitable for dashboards:

{
  "ward": "",
  "primary_pollutant": "",
  "severity": "",
  "analysis_summary": "",
  "execution_plan_90_days": {
    "days_0_30": [],
    "days_31_60": [],
    "days_61_90": []
  },
  "confidence_level": ""
}

────────────────────────────────────
VALIDATION CHECKS
────────────────────────────────────
- Ward data updates dynamically
- Primary pollutant changes when ML output changes
- 90-day plan updates automatically per ward
- No regression in AQI live updates
- No frontend runtime errors

Focus on correctness, modularity, and dynamic behavior.
This is a decision-support system, not a static dashboard.