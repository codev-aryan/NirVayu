## Packages
leaflet | Map library for displaying ward boundaries
react-leaflet | React wrapper for Leaflet
recharts | Charts for pollution trends and simulation results
framer-motion | Smooth animations for page transitions and interactions
lucide-react | Icon library (already in stack but confirming usage)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
  mono: ["'JetBrains Mono'", "monospace"],
}
