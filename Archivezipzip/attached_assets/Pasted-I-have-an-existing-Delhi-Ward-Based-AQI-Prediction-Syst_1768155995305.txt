I have an existing Delhi Ward-Based AQI Prediction System written in Python (Jupyter Notebook style).
It already contains:
- Ward metadata (lat/lon, zones)
- CPCB / AQI API integration
- Feature engineering
- ML models (RandomForest, XGBoost, LSTM)
- Existing API keys and environment setup

I want to INTEGRATE the AQI PREDICTION MODEL into the backend in a SAFE and NON-DESTRUCTIVE way.

========================
STRICT NON-NEGOTIABLE RULES
========================
1. DO NOT:
   - Modify existing API keys
   - Change CPCB / AQI API logic
   - Rename existing classes, functions, or variables
   - Delete or refactor notebook logic
   - Hardcode any secrets
   - Replace real AQI with predicted AQI

2. Existing AQI fetching MUST remain the source of truth.

========================
WHAT TO ADD (ONLY ADD)
========================
1. Extract ONLY the AQI prediction logic into a new module:
   - File name: aqi_predictor.py

2. This module must:
   - Load the already-trained ML model (or train if missing)
   - Accept structured input:
     {
       current_aqi,
       pm25,
       pm10,
       no2,
       timestamp,
       ward_id
     }

3. Expose ONE function only:
   predict_future_aqi(input_data)

4. Function output format:
   {
     "predicted_aqi": float,
     "confidence": float,
     "prediction_horizon": "6h" or "24h"
   }

========================
INTEGRATION RULES
========================
- Prediction is OPTIONAL and ADDITIVE
- If model fails:
  - Return predicted_aqi = null
  - Log error silently
  - System must continue running

- Predicted AQI is:
  - For advisory & visualization only
  - NOT used for enforcement
  - NOT used in CO2 budget or policy logic

========================
API SAFETY
========================
- Use existing AQI data as model input
- Use process.env for any secrets
- Do not expose keys in logs or frontend

========================
DESIGN PHILOSOPHY
========================
Treat this as a government-grade early warning system:
- Fail-safe
- Conservative
- Backward compatible
- No breaking changes

Proceed carefully and minimally.
